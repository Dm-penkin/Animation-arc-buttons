import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bar: {
    height: 30,
    width: 300,
  },
  container: {
    flexDirection: 'row',
    overflow: 'hidden',
    width: '100%',
  },
})